{
  "name": "ai-anomaly-war-room",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "build:offline": "cross-env OFFLINE=1 vite build --outDir dist-offline"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vite-plugin-singlefile": "^2.0.2"
  }
}
