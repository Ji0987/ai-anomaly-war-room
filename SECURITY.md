# 安全與資料處理基本規範

對應課程評分項 8(5 分)。

## 機密管理

- 密碼、API Key、Token、資料庫連線字串一律不寫入程式碼、不提交至 Git。
- Dify 僅在開發階段離線批次使用;其 API Key 只存在本機環境變數,repo 內不出現。
- 若不慎提交機密:立即撤銷該金鑰 → 通知全組 → 以新金鑰替換,不要只刪檔案(歷史仍在)。

## 資料權限與保存

- 本專案所有產線資料均為**自造模擬資料**,不含任何真實企業、個人或製程機密。
- 情境資料(`src/data/scenarios/`)版本化管理,修改需經 PR 審查。
- 使用者操作僅存於瀏覽器 `localStorage`,不上傳、不追蹤;「重設」即清除。

## 競賽匿名規範

- UI、HTML metadata、文件輸出不得出現學校、組員姓名或企業名稱/標誌。
- GitHub Pages 網址含可識別帳號時,競賽送審一律改交離線單檔版。
